/**
 * 文本水平对其方式与 flex 对其方式映射
 */
export enum horizontalAlignFlexMap {
  left = 'flex-start',
  center = 'center',
  right = 'flex-end',
}

/**
 * 文本垂直对其方式与 flex 对其方式映射
 */
export enum verticalAlignFlexMap {
  top = 'flex-start',
  center = 'center',
  bottom = 'flex-end',
}
